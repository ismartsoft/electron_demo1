{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "node",
            "request": "launch",
            "name": "Electron Main",
            //"stopOnEntry": false,
            //"sourceMaps": true,
            "runtimeExecutable": "${workspaceRoot}/node_modules/.bin/electron",
            "protocol": "inspector",
            "program": "${workspaceRoot}/src/main.js"
        }
    ]
}
